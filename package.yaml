name:                havoc
version:             0.1.0.0
license:             BSD3
author:              "Alex Collins"
maintainer:          "atcollins@pm.me"
copyright:           "2019 Alex Collins"

extra-source-files:
- README.md

description:         Please see the README

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src
  dependencies:
    - aeson
    - base >=4.7 && <5
    - bytestring
    - case-insensitive
    - http-client
    - http-reverse-proxy
    - http-types
    - mtl
    - mwc-random
    - stm
    - wai
    - warp

executables:
  havoc:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - aeson
    - async
    - havoc
    - bytestring
    - raw-strings-qq
    - text
    - havoc

tests:
  havoc-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - havoc
